import { Nuosu_SIL } from "next/font/google";

/**
 * Fonte dos títulos da landing. Escolhida pelo user — estilo serif
 * orgânico que dá peso visual aos headings.
 */
export const titleFont = Nuosu_SIL({
  subsets: ["latin"],
  weight: "400",
  display: "swap",
});
